version: '2'
services:
    web:
        image: nginx:alpine
        container_name: web
        volumes:
            - /www/wwwroot/laravel-shop:/var/www/html
            - /www/docker/etc/site:/etc/nginx/conf.d
            - /www/docker/etc/nginx/nginx.conf:/etc/nginx/nginx.conf
            - ./log/:/var/log/dnmp
        ports:
            - "80:80"
            - "443:443"
        restart: always
    php:
        build: 
            context: /www/docker/etc/php
            args: 
                PHP_VERSION: latest
        container_name: php
        restart: always
        environment:
            LANG: C.UTF-8
        volumes:
            - /www/docker/etc/php/php.ini:/usr/local/etc/php/conf.d/php.ini
            - /www/wwwroot/laravel-shop:/var/www/html
    myadmin:
        image: phpmyadmin/phpmyadmin
        container_name: myadmin
        ports:
            - "8080:80"
        environment:
            - PMA_ARBITRARY=1
        restart: always
        depends_on:
            - mysql
    mysql:
        image: mysql:5.7.22
        container_name: mysql
        restart: always
        ports:
            - "127.0.0.1:3306:3306"
        environment:
            MYSQL_ROOT_PASSWORD: root
            MYSQL_USER: dean
            MYSQL_USER_PASSWORD: dean
            MYSQL_DATABASE: laravel_shop
        volumes:
            - /www/docker/db/mysql:/var/lib/mysql
            - /www/docker/etc/mysql/mysql.cnf:/etc/mysql/conf.d/mysql.cnf
    redis:
        image: redis:latest
        container_name: redis
        ports:
            - "127.0.0.1:6379:6379"
        restart: always
        volumes:
            - /www/docker/etc/redis/redis.conf:/usr/local/etc/redis/redis.conf:ro
            - /www/docker/db/redis/:/data/:rw
        command: redis-server /usr/local/etc/redis/redis.conf
    mailhog:
        image: mailhog/mailhog
        container_name: mailhog
        ports:
            - "8025:8025"
            - "1025:1025"
        restart: always
    es:
        build:
          context: /www/docker/etc/elasticsearch
        container_name: es
        environment:
            - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
        restart: always
        volumes:
            - /www/docker/db/es:/usr/share/elasticsearch/data
        ports:
            - 127.0.0.1:9200:9200